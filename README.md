## ft_printf
Ce projet consiste à reproduire le fonctionnement de la fonction printf() en C dans laquelle les conversions suivantes sont implémenter: %c, %s, %p, %d, %i, %u, %x, %X et %%. Pour la partie bonus: "-0." et "# +".
